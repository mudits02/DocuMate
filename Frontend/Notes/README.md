# Learning Notes

1. Will be using **Git Tree API** instead of **Contents API** because it supports for larger file structures and it is better to use this instead of going with the older approach. <br><br>
2. New **React Hooks** discovered and used , all **react-router-dom**,
   1. useLocation()
   2. useParams()
   3. useNavigate()
   4. useSearchParams()